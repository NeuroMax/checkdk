export interface IClient {
    title?: string,
    description?: string,
    url?: string,
    enabled?: boolean,
    key?: string,
    date_create?: Date,
    last_activity?: Date,
    control_center?: boolean,
    old_url?: string,
    background?: string
}